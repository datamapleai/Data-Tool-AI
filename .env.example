# Database
DATABASE_URL="postgresql://user:password@localhost:5432/data_tool_ai"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-here-change-in-production"

# App encryption key (for BYOK API key encryption)
# Generate with: openssl rand -hex 32
APP_ENCRYPTION_KEY="your-encryption-key-here-change-in-production"

# Optional: Admin setup via environment (skips wizard steps)
# ADMIN_EMAIL=""
# ADMIN_PASSWORD=""
